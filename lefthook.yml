pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,ts,jsx,tsx}'
      run: yarn run rome check {staged_files}
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: yarn run commitlint --edit
